<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>cc.cloudpivot.starter</groupId>
        <artifactId>cloudpivot-starter-dependencies</artifactId>
        <version>1.0.0</version>
        <relativePath>./cloudpivot-starter-dependencies</relativePath>
    </parent>

    <artifactId>cloudpivot-starter</artifactId>
    <packaging>pom</packaging>

    <name>CloudPivot Starter</name>
    <description>
        CloudPivot Starter 基于“约定优于配置”理念，进一步精简常规配置，提供完整的配置解决方案，帮助开发人员更快速地将常用第三方库或工具集成到 Spring Boot Web 应用程序中。
        CloudPivot Starter 封装了一系列经过企业实践验证的依赖包（如 MyBatis-Plus、SaToken），可轻松集成到应用中，减少开发人员手动引入依赖及配置的工作量，为 Spring Boot Web 项目的灵活快速构建提供支持。
        CloudPivot Starter 源自 CloudPivot Admin 后台管理框架项目。随着 CloudPivot Admin 的发展，作者发现初学者需要关注过多的脚手架通用基础能力，且在新项目中复用这些能力时存在迁移困难的问题。于是，CloudPivot Starter 应运而生，作者在 CloudPivot Admin 2.x 版本时，将项目中的通用基础能力进行了抽离并深度优化。这样，无论是在 CloudPivot Admin 中使用，还是单独使用这些基础能力，都可以更加轻松。
    </description>

    <properties>
        <!-- Maven Environment Versions -->
        <java.version>17</java.version>
        <resource.delimiter>@</resource.delimiter>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <maven.javadoc.failOnError>false</maven.javadoc.failOnError>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <modules>
        <module>cloudpivot-starter-dependencies</module>
        <module>cloudpivot-starter-bom</module>
        <module>cloudpivot-starter-core</module>
        <module>cloudpivot-starter-json</module>
        <module>cloudpivot-starter-api-doc</module>
        <module>cloudpivot-starter-validation</module>
        <module>cloudpivot-starter-web</module>
        <module>cloudpivot-starter-cache</module>
        <module>cloudpivot-starter-auth</module>
        <module>cloudpivot-starter-data</module>
        <module>cloudpivot-starter-encrypt</module>
        <module>cloudpivot-starter-security</module>
        <module>cloudpivot-starter-ratelimiter</module>
        <module>cloudpivot-starter-idempotent</module>
        <module>cloudpivot-starter-trace</module>
        <module>cloudpivot-starter-captcha</module>
        <module>cloudpivot-starter-messaging</module>
        <module>cloudpivot-starter-log</module>
        <module>cloudpivot-starter-excel</module>
        <module>cloudpivot-starter-storage</module>
        <module>cloudpivot-starter-license</module>
        <module>cloudpivot-starter-extension</module>
    </modules>

    <build>
        <plugins>
            <!-- 编译插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgument>-parameters</compilerArgument>
                </configuration>
            </plugin>
            <!-- 代码格式化插件 -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>apply</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <java>
                        <removeUnusedImports/>
                        <eclipse>
                            <file>.style/p3c-codestyle.xml</file>
                        </eclipse>
                        <licenseHeader>
                            <file>.style/license-header</file>
                        </licenseHeader>
                    </java>
                </configuration>
            </plugin>
            <!-- 统一版本号插件 -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>oss</flattenMode>
                    <pomElements>
                        <parent>expand</parent>
                        <properties>keep</properties>
                    </pomElements>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten-clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <url>https://github.com/CloudPivot/cloudpivot-starter</url>
    <licenses>
        <license>
            <name>GNU LESSER GENERAL PUBLIC LICENSE</name>
            <url>http://www.gnu.org/licenses/lgpl.html</url>
        </license>
    </licenses>
    <developers>
        <developer>
            <id>CloudPivot</id>
            <name>CloudPivot</name>
            <email>system@cloudpivot.CC</email>
            <roles>
                <role>Creator</role>
                <role>Java Development Engineer</role>
            </roles>
            <timezone>+8</timezone>
            <url>https://github.com/CloudPivot</url>
        </developer>
    </developers>
    <scm>
        <connection>scm:git:git@github.com:CloudPivot/cloudpivot-starter.git</connection>
        <developerConnection>scm:git:git@github.com:CloudPivot/cloudpivot-starter.git</developerConnection>
        <url>https://github.com/CloudPivot/cloudpivot-starter</url>
    </scm>
</project>
